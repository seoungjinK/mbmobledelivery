<UserControl x:Class="MBDManager.Views.LiveMonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.expression.blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:MBDManager.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="900" d:DesignWidth="1700"
             Background="#333">

    <UserControl.Resources>
        <DataTemplate x:Key="BoxTemplate">
            <Rectangle Stroke="Red" StrokeThickness="3"
                       Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}"
                       Width="{Binding Width}" Height="{Binding Height}"/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <UniformGrid Grid.Column="0" Columns="2" Rows="2" Margin="10">

            <Border Background="Black" CornerRadius="5" Margin="5" BorderBrush="#444" BorderThickness="1">
                <Grid>
                    <Image Source="{Binding TopCameraFrame}" Stretch="Uniform"/>
                    <TextBlock Text="Top Camera (Main)" Foreground="Lime" FontWeight="Bold" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Effect="{StaticResource MaterialDesignShadowDepth1}"/>
                    <Canvas>
                        <ItemsControl ItemsSource="{Binding TopCameraOnnxResults}" ItemTemplate="{StaticResource BoxTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </Canvas>
                </Grid>
            </Border>

            <Border Background="Black" CornerRadius="5" Margin="5" BorderBrush="#444" BorderThickness="1">
                <Grid>
                    <Image Source="{Binding SideCamera1Frame}" Stretch="Uniform"/>
                    <TextBlock Text="Side Camera 1" Foreground="White" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Effect="{StaticResource MaterialDesignShadowDepth1}"/>
                    <Canvas>
                        <ItemsControl ItemsSource="{Binding Side1OnnxResults}" ItemTemplate="{StaticResource BoxTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </Canvas>
                </Grid>
            </Border>

            <Border Background="Black" CornerRadius="5" Margin="5" BorderBrush="#444" BorderThickness="1">
                <Grid>
                    <Image Source="{Binding SideCamera2Frame}" Stretch="Uniform"/>
                    <TextBlock Text="Side Camera 2" Foreground="White" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Effect="{StaticResource MaterialDesignShadowDepth1}"/>
                    <Canvas>
                        <ItemsControl ItemsSource="{Binding Side2OnnxResults}" ItemTemplate="{StaticResource BoxTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </Canvas>
                </Grid>
            </Border>

            <Border Background="#222" CornerRadius="5" Margin="5" BorderBrush="#333" BorderThickness="1">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="CameraOff" Width="50" Height="50" Foreground="Gray"/>
                    <TextBlock Text="No Signal" Foreground="Gray" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

        </UniformGrid>

        <StackPanel Grid.Column="1" Margin="10">
            <Button Command="{Binding CaptureAndInspectCommand}" 
                    Content="📸 3채널 AI 정밀 검사" 
                    Height="80" Margin="0,0,0,20"
                    FontSize="20" FontWeight="Bold"
                    Background="#D32F2F" Foreground="White"
                    materialDesign:ButtonAssist.CornerRadius="10"/>

            <materialDesign:Card Padding="20" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="QR 디코딩 결과" FontSize="16" Foreground="Gray" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding CurrentQrCode}" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </materialDesign:Card>

            

            <materialDesign:Card Padding="20" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="최종 불량 판정" FontSize="16" Foreground="Gray" Margin="0,0,0,5"/>

                    <TextBlock Text="{Binding CurrentDefectType}" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="{Binding DefectColor}"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Padding="20" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="팔레트 상태" FontSize="16" Foreground="Gray" Margin="0,0,0,15"/>
                    <TextBlock Text="{Binding CurrentPaletteStatus}" FontSize="32" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding CurrentPaletteCount}" Maximum="4" Height="10" Margin="0,10,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>   
    </Grid>
</UserControl>