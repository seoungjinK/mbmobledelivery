<UserControl x:Class="MBDManager.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MBDManager.Views"
             xmlns:vm="clr-namespace:MBDManager.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:SettingsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="900" d:DesignWidth="1700">

    <Grid>
        <StackPanel MaxWidth="800" HorizontalAlignment="Left" Margin="10">

            <materialDesign:Card Margin="10" Padding="25" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="카메라 번호 설정 (0, 1, 2...)" FontSize="20" FontWeight="Medium" Margin="0,0,0,20"/>

                    <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             materialDesign:HintAssist.Hint="상단 메인 카메라 (Top) 번호"
                             Text="{Binding CurrentSettings.TopCameraIndex}" Margin="0,10"/>

                    <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             materialDesign:HintAssist.Hint="측면 카메라 1 (Side 1) 번호"
                             Text="{Binding CurrentSettings.SideCamera1Index}" Margin="0,10"/>

                    <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             materialDesign:HintAssist.Hint="측면 카메라 2 (Side 2) 번호"
                             Text="{Binding CurrentSettings.SideCamera2Index}" Margin="0,10"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Margin="10" Padding="25" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="서버 및 AI 설정" FontSize="20" FontWeight="Medium" Margin="0,0,0,20"/>

                    <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             materialDesign:HintAssist.Hint="서버 주소 (예: http://127.0.0.1:5000)"
                             Text="{Binding CurrentSettings.ServerUrl}" Margin="0,10"/>

                    <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             materialDesign:HintAssist.Hint="AGV IP 주소 (예: 192.168.1.106)"
                             Text="{Binding CurrentSettings.AgvIpAddress}" Margin="0,10"/>

                    <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             materialDesign:HintAssist.Hint="아두이노 포트 (예: COM3)"
                             Text="{Binding CurrentSettings.ArduinoPort}" Margin="0,10"/>

                    <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             materialDesign:HintAssist.Hint="ONNX 모델 파일명 (예: best9.onnx)"
                             Text="{Binding CurrentSettings.OnnxModelPath}" Margin="0,10"/>
                </StackPanel>
            </materialDesign:Card>

            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding SaveSettingsCommand}"
                    Margin="10" 
                    HorizontalAlignment="Right" 
                    Width="200" 
                    Height="50">

                <TextBlock Text="설정 저장"
                           FontFamily="Malgun Gothic"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Button>

        </StackPanel>
    </Grid>
</UserControl>